;
; BIND data file for local loopback interface
;
$TTL 20
@ IN SOA domain.tld. root.domain.tld. (
         2  ; Serial
         20 ; Refresh
         20 ; Retry
         20  ; Expire
         20 ) ; Negative Cache TTL
;
@ IN NS domain.tld.
@ IN A 172.20.128.2
www IN A 172.20.128.2

